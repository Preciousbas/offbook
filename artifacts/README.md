# Artifacts

Run outputs land here (`claims.json`, `findings.json`, `report.md`, `evidence/`).

Gitignored by default. Regenerate:

```bash
offbook audit --target owned_fix --dry-run
```

Checked-in samples live under `samples/`.
